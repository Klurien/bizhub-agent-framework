{
  "name": "bizhub",
  "version": "1.0.0",
  "private": true,
  "description": "BizHub Agent Framework — Build AI agents that manage your marketplace",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "npm run test -ws",
    "lint": "npm run lint -ws",
    "clean": "npm run clean -ws"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.7.0"
  }
}
